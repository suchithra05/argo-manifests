apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: voting-app
  labels:
    app: voting-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: voting-app
  template:
    metadata:
      labels:
        app: voting-app
    spec:
      containers:
        - name: voting-app
          image: suchithra05/voting-app:latest
          ports:
            - containerPort: 8080
          env:
            - name: REDIS_HOST
              value: "redis"
  strategy:
    blueGreen:
      activeService: voting-app-active
      previewService: voting-app-preview
      autoPromotionEnabled: false
